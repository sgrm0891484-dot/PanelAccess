# =========================================================
# AEGIS // DEFENSE - ENVIRONMENT CONFIGURATION
# =========================================================

# Production PostgreSQL Database URL (Supabase or direct PostgreSQL connection string)
DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres

# Supabase Credentials (Optional if DATABASE_URL is provided)
SUPABASE_URL=https://[PROJECT_ID].supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Server-Side Authentication Secret for Secure Cryptographic Token Generation
AUTH_SECRET=aegis-quantum-auth-secret-key-2026-production

# Master Admin Authentication Credentials (Validated server-side only)
ADMIN_ID=ADMINXD
ADMIN_PASS_KEY=ADMIN5921N

# Node / Express Server Runtime
NODE_ENV=production
